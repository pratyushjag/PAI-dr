# Frontend — AI Document Reader

React (Vite) single-page app. See the [main README](../README.md) in the repository
root for full setup, architecture, and deployment documentation.

## Quick start

    npm install
    npm run dev

Requires `VITE_API_BASE_URL` pointing at the backend (see root README).